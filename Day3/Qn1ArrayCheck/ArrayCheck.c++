#include<iostream>
using namespace std;
class Solution {
  public:
    bool checkEqual(vector<int>& a, vector<int>& b) {
        // code here
        
    }
};

// two approach  first one sort the both array then check one by one 


brute force approahc is O(n^2)


ok then 

using hashmap i have to solve then i will get O(n) time and space complexity so 

done logic_error